#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string codename;
    if (!getline(cin, codename)) return 0;

    cout << "Signal: " << codename << '\n';
    return 0;
}
