#include <bits/stdc++.h>
using namespace std;

int main() {
    string callsign;
    long long charge;
    if (!(cin >> callsign >> charge)) return 0;
    cout << "callsign=" << callsign << "\n";
    cout << "charge=" << charge << "\n";
    return 0;
}