# Variable Base: Core Allocation

## Statement

Read two integers: `total` and `used`.

You are guaranteed `0 <= used <= total`.

Print exactly:

- `remaining=<total-used>`
- `double_remaining=<2*(total-used)>`

## Constraints

- `0 <= total <= 10^9`
- `0 <= used <= total`