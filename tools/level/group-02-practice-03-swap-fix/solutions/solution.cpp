#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long left, right;
    if (!(cin >> left)) return 0;
    if (!(cin >> right)) return 0;

    cout << "left=" << right << '\n';
    cout << "right=" << left << '\n';
    return 0;
}