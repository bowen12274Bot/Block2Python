# Variable Base: Reactor Reset

## Statement

Read three integers `base`, `delta`, `factor`.

Compute `after = base + delta`.

Print exactly:

- `after_delta=<after>`
- `amplified=<after*factor>`

## Constraints

- `-10^9 <= base, delta <= 10^9`
- `0 <= factor <= 10^5`