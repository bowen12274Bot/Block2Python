# Variable Base: Refuel Rate

## Statement

Read two integers `fuel` and `hours` where `hours > 0`.

Print exactly:

- `per_hour=<fuel / hours>`
- `remainder=<fuel % hours>`

(Use integer division.)

## Constraints

- `0 <= fuel <= 10^9`
- `1 <= hours <= 10^6`