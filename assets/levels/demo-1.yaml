level_id: demo-1
title: 兩數相加（Stub）
prompt: 讀入兩個整數並輸出相加結果。

learning_markdown: |
  目標：熟悉 input/print 與整數加法。

  提示：可以用 `a, b = map(int, input().split())` 讀入兩個數。

story_intro_markdown: 你收到一個任務：幫基地計算補給箱的總數。
story_outro_markdown: 任務完成！你解鎖了下一關。

testcases:
  - name: basic
    stdin: |
      1 2
    expected_stdout: |
      3

next_level_ids:
  - add-two-numbers

block_schema_version: TBD

metadata:
  stage: prototype
  track: demo-flow
  stub_judge:
    status: AC
    summary: "StubJudge: forced AC for demo-1."
  stub_analysis:
    status: PASS
    summary: "StubAnalyzer: forced PASS for demo-1."
  block_schema_version_note: Schema not fixed yet; keep using metadata until decided.
