# PowerShell 指令問題

## `&&` 不可用

常見症狀：

- `&&` 被 PowerShell 視為無效分隔符號

處理方式：

- 把 `git add` 和 `git commit` 拆成兩個獨立指令
- 不要沿用 bash 習慣把多步驟硬串在一起

## 排錯原則

- 優先用 PowerShell 相容語法
- 若同一動作需要多步驟，分開執行比硬串命令更穩
- 若需要傳遞帶空白的參數，優先明確加上引號
